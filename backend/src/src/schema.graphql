type Query {
  meta: Meta!
  testJson: Int!
  testPkl: [AvgWinds]
}

type AvgWinds {
  dir: String!
  vel: Int!
  velName: String!
  count: Int!
}

"""
Some backend information
"""
type Meta {
  ciPipelineId: String!
  buildDate: String!
}
